export default function DropsPage() {
  return (
    <div className="min-h-screen p-6">
      <h1 className="text-3xl font-black">Drops</h1>
      <p className="text-sm text-text-muted mt-3 max-w-xl">
        Exclusive drops are not wired to a live table. Demo drop calendars were removed so this page cannot show fake inventory.
      </p>
    </div>
  );
}
